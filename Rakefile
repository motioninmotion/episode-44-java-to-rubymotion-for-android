# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotionPre/lib")
require 'motion/project/template/android'

Motion::Project::App.setup do |app|
  app.name = 'My First App'
  app.package = 'tv.motioninmotion.myfirstapp'
  app.api_version = '19'
end
